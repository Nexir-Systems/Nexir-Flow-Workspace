import type { SxProps, Theme } from '@mui/material';

/** Inline link between Sign in ↔ Create account — body scale, not a second headline. */
export const authAccountSwitchLinkSx: SxProps<Theme> = {
  fontWeight: 600,
  fontSize: '1rem',
  lineHeight: 1.5,
};
